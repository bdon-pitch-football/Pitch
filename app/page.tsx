import Site from '@/components/site/Site';

// The pre-registration site (BUZ, 15 Sep). components/coming-soon stays in the
// repo, unused, so going back is a one-line change.
export default function Home() {
  return <Site />;
}
